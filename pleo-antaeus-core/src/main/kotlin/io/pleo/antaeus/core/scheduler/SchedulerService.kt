package io.pleo.antaeus.core.scheduler

import org.quartz.Job
import org.quartz.JobExecutionContext

class SchedulerService : Job {
    override fun execute(context: JobExecutionContext?) {
        TODO("Not yet implemented")
    }
}